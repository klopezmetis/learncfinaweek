{
  "configurations": [
    {
      "name": "Debug CommandBox",
      "type": "cfml",
      "request": "attach",
       // make sure this is the same value you configured your server with
      "serverPort": 8888
    }
  ]
}